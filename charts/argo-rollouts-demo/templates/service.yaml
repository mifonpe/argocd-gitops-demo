apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-rollout-demo.fullname" . }}
  labels:
    {{- include "argo-rollout-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argo-rollout-demo.selectorLabels" . | nindent 4 }}
